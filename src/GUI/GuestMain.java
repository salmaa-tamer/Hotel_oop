
/*package GUI;

import GuestandRoomSystem.HotelDatabase;
import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.stage.Stage;

    public class GuestMain extends Application {
        @Override
        public void start(Stage stage) throws Exception {

            HotelDatabase.loadDummyData();
            HotelDatabase.currentGuest = HotelDatabase.guests.get(0);

            Parent root = FXMLLoader.load(
                    getClass().getResource("/GUI/FXML/GuestDashboard.fxml")
            );
            Scene scene = new Scene(root);
            stage.setScene(scene);
            stage.setTitle("Hotel Reservation - Guest");
            stage.show();
        }

        public static void main(String[] args) {
            launch(args);
        }
    }

*/
